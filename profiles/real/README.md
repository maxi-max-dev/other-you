# profiles/real/

这个目录放**真实自我**的处境摘要：从个人笔记里手工蒸馏出来的几句话（处境、资源、软肋、当前纠结），不是原始笔记本身。

喂进推演管线之前，本人要先过目这份摘要，确认没有失真或误读。

整个目录已被 `.gitignore` 保护，除本说明文件外，其余任何文件都不会进 git。

用这一档跑出来的推演产物同样不进 git：文件落在 `runs/real/`，注册在 `runs/real-manifest.js`，两处都被 `.gitignore` 挡住。网页选择页的「真实自我」分区因此只在本机有内容，公开站上永远是空的。
